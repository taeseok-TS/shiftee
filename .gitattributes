# 리눅스 서버에서 그대로 실행되는 파일은 LF 로 고정한다.
# ⚠ CRLF 로 배포하면 `#!/usr/bin/env python3` 가 되어 "그런 파일 없음" 이라는
#   엉뚱한 오류가 난다 — 장애 대응 중에 최후 방어선을 진단하려는 바로 그 순간에(2026-09-04).
*.py text eol=lf
*.sh text eol=lf
deploy/watchdog/*.service text eol=lf
deploy/watchdog/*.timer   text eol=lf
